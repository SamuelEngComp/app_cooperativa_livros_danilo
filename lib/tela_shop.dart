import 'package:flutter/material.dart';

class TelaShop extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Tela em desenvolvimento... aguarde só mais um pouco'),
      ),

    );
  }
}
